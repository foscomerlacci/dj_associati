============
Installation
============

At the command line::

    $ easy_install dj_associati

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv dj_associati
    $ pip install dj_associati
